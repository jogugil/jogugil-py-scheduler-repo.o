apiVersion: v1
kind: Pod
metadata:
  generateName: test-nginx-pod-
  namespace: test-scheduler
  labels:
    app: test-nginx
spec:
  containers:
  - name: nginx
    image: nginx
  schedulerName: my-scheduler
  restartPolicy: Never
